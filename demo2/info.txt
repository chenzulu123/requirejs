requirejs的模块依赖
当我们遇到模块依赖问题的时候，推荐的处理方式为：
// 推荐做法：依赖模块路径的时候，把所有有返回值的模块放在前面，把所有无返回值的模块放在后面
// -->a、c、d：有返回值 b、e：无返回值
// require(['a','c','d','b','e'],function(a,c,d){});
//这样写的可以省略那些没有返回值模块的形参的定义，如果这些无返回值的模块写在前面的话，而没有定义形参，在使用模块依赖的时候会出错
